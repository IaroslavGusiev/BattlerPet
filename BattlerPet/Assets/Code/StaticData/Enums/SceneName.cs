namespace Code.Data
{
    public enum SceneName
    {
        // Enum should have same spelling as scene name, cause it will be parse to string
        
        BootstrapScene = 0,
        BattleArea = 1,
        ShowWindowsScene = 2,
        
        // last index = 2
    }
}