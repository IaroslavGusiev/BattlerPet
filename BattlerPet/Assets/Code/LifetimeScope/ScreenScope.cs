using VContainer;
using VContainer.Unity;

namespace Code.LifetimeScopes
{
    public class ScreenScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
            
        }
    }
}